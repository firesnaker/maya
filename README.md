# maya
Personal Assistant

The apps are containerized using Docker, simply run ./deploy.sh remember to set it to executable (+x).
When finished, run docker compose down

For development, you can run ollama using:
docker compose up -d ollama

and stop is after using
docker compose stop ollama
