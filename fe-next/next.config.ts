import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  //This line enables the generation of the .next/standalone directory
  output: 'standalone',
};

export default nextConfig;
